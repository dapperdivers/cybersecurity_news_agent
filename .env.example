# Claude Code OAuth Token (required)
# Get your token from: https://console.anthropic.com/
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-oat01-your-token-here

# Runtime UID/GID mapping for proper file permissions
# Set these to match your host user to avoid permission issues with mounted volumes
# Defaults to 1000 if not set
# USER_UID=1000
# USER_GID=1000
#
# To automatically match your host user, set:
# USER_UID=$(id -u)
# USER_GID=$(id -g)

# Optional: Override default paths (already set in Dockerfile with sensible defaults)
# DEFAULT_FEEDS_PATH=/app/mcp-servers/rss_fetcher/config/default_feeds.json
# OUTPUT_DIR=/app/outputs
